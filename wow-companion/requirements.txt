###############################
# WoW Companion Requirements  #
###############################
# Notes:
# - Prefer Python 3.11 or 3.12 for best binary wheel coverage (OpenCV, torch, keyboard).
# - Python 3.13 wheels for some packages may lag; if install fails, install Python 3.12 and recreate venv.
# - Versions are minimally pinned (upper bounds avoided unless needed) to ease cross‑platform installs.
# - Optional / heavy or dev dependencies are separated below.

## Core Runtime (screen capture, CV, OCR, state, reasoning helpers)
numpy>=1.26,<3
opencv-python>=4.9.0; python_version < '3.13'  # (If on 3.13 and this fails, use Python 3.12)
pytesseract>=0.3.10
mss>=9.0.1
Pillow>=10.3
pydantic>=2.7,<3
keyboard>=0.13.5; platform_system == 'Windows'
pywin32>=306; platform_system == 'Windows'
rich>=13.7
requests>=2.32

## Audio / TTS (optional; skip if issues)
sounddevice>=0.4.7; platform_system == 'Windows'
pyttsx3>=2.90
pydub>=0.25.1

## Learning / Analytics (optional; lightweight)
scikit-learn>=1.4
matplotlib>=3.8

## Optional: Local model experimentation (comment out if not needed)
torch>=2.3.0; platform_system == 'Windows'

## (Planned) API server / remote control (not used in main loop yet)
fastapi>=0.111.0
uvicorn>=0.30.0

## Dev / Testing
pytest>=8.2

# End of file
